# ============================================================================
# ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env and fill in your values
#
# ⚠️  SECURITY WARNING:
# All variables prefixed with VITE_ are PUBLIC and bundled into client code.
# Never use VITE_ prefix for secrets, API keys, or sensitive data.
# ============================================================================

# Supabase PUBLIC configuration
# Get these from: Supabase Dashboard > Settings > API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Auth mode
# Set to "true" for local development (uses mock auth, no LinkedIn needed)
# Set to "false" for production (requires LinkedIn OAuth configured in Supabase)
VITE_USE_MOCK_AUTH=true

# ============================================================================
# SERVER-SIDE ONLY (configured in Supabase Dashboard, NOT here)
# ============================================================================
# LinkedIn OAuth credentials are stored in Supabase, not in client code:
#   Supabase Dashboard > Authentication > Providers > LinkedIn
#
# ⚠️  NEVER put these in VITE_ variables - they would be exposed to users!
# - LINKEDIN_CLIENT_ID (server-side only)
# - LINKEDIN_CLIENT_SECRET (server-side only)
# ============================================================================
